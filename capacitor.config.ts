import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fr.dymension.delyss',
  appName: 'Delyss menu',
  webDir: '.output/public',
  bundledWebRuntime: false
};

export default config;